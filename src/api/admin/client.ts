/**
 * AUTO-GENERATED - DO NOT EDIT
 * Generated by Claude from backend Go code
 * Source: orris backend
 * Last updated: 2026-02-28
 *
 * Admin Traffic Statistics API Client
 * All endpoints require admin authentication.
 */

import { apiClient } from '@/shared/lib/axios';
import type { APIResponse, ListResponse } from '@/shared/types/api.types';
import type {
  AdminDashboardResponse,
  TrafficStatsQueryParams,
  TrafficRankingParams,
  TrafficTrendParams,
  TrafficOverview,
  UserTrafficStatsItem,
  SubscriptionTrafficStatsItem,
  NodeTrafficStatsItem,
  TrafficRanking,
  TrafficTrend,
  AdminBindingStatusResponse,
  AdminTelegramBindingResponse,
  UpdateAdminPreferencesRequest,
  CategorySettingsResponse,
  UpdateCategorySettingsRequest,
  SystemSettingsResponse,
  UpdateSystemSettingsRequest,
  OAuthSettingsResponse,
  UpdateOAuthSettingsRequest,
  EmailSettingsResponse,
  UpdateEmailSettingsRequest,
  EmailTestRequest,
  EmailTestResponse,
  SetupStatusResponse,
  TelegramConfigResponse,
  UpdateTelegramConfigRequest,
  TelegramTestResult,
  // Admin Subscription Management types (Added 2026-01-29, Updated 2026-02-28)
  AdminSubscriptionListParams,
  AdminSubscriptionDTO,
  AdminCreateSubscriptionRequest,
  AdminCreateSubscriptionResponse,
  AdminUpdateSubscriptionRequest,
  SuspendSubscriptionRequest,
  RenewSubscriptionRequest,
  ChangePlanRequest,
  // Note: Admin External Forward Rule types have been removed (2026-01-20)
  // External forward rules are now part of the unified ForwardRule model
} from './types';

// ========== Admin Dashboard API (Added 2026-02-11) ==========

/**
 * Get admin dashboard overview
 * GET /admin/dashboard
 * @requires Authentication, Admin
 *
 * Returns a snapshot of key operational metrics including users,
 * subscriptions, nodes, forward agents, and today's traffic.
 *
 * @returns Dashboard metrics snapshot
 *
 * @example
 * ```typescript
 * const dashboard = await getAdminDashboard();
 * console.log('Total users:', dashboard.users.total);
 * console.log('Active subs:', dashboard.subscriptions.active);
 * console.log('Online nodes:', dashboard.nodes.online);
 * console.log('Today traffic:', formatTrafficBytes(dashboard.trafficToday.total));
 * ```
 */
export const getAdminDashboard = async (): Promise<AdminDashboardResponse> => {
  const response = await apiClient.get<APIResponse<AdminDashboardResponse>>(
    '/admin/dashboard'
  );
  return response.data.data;
};

// ========== Traffic Statistics APIs (Admin only) ==========

/**
 * Get global traffic overview
 * GET /admin/traffic-stats/overview
 * @requires Authentication, Admin
 *
 * Returns platform-wide traffic statistics for the specified time range.
 *
 * @param params - Query parameters with time range
 * @returns Traffic overview statistics
 *
 * @example
 * ```typescript
 * const overview = await getTrafficOverview({
 *   from: '2025-01-01T00:00:00Z',
 *   to: '2025-01-31T23:59:59Z',
 * });
 * console.log(formatTrafficBytes(overview.totalTraffic));
 * ```
 */
export const getTrafficOverview = async (
  params: TrafficStatsQueryParams
): Promise<TrafficOverview> => {
  const response = await apiClient.get<APIResponse<TrafficOverview>>(
    '/admin/traffic-stats/overview',
    { params }
  );
  return response.data.data;
};

/**
 * Get traffic statistics by user
 * GET /admin/traffic-stats/users
 * @requires Authentication, Admin
 *
 * Returns paginated traffic statistics grouped by user.
 *
 * @param params - Query parameters with time range and pagination
 * @returns Paginated list of user traffic statistics
 *
 * @example
 * ```typescript
 * const userStats = await getUserTrafficStats({
 *   from: '2025-01-01T00:00:00Z',
 *   to: '2025-01-31T23:59:59Z',
 *   page: 1,
 *   pageSize: 20,
 * });
 * userStats.items.forEach(user => {
 *   console.log(`${user.userEmail}: ${formatTrafficBytes(user.total)}`);
 * });
 * ```
 */
export const getUserTrafficStats = async (
  params: TrafficStatsQueryParams
): Promise<ListResponse<UserTrafficStatsItem>> => {
  const response = await apiClient.get<APIResponse<ListResponse<UserTrafficStatsItem>>>(
    '/admin/traffic-stats/users',
    { params }
  );
  return response.data.data;
};

/**
 * Get traffic statistics by subscription
 * GET /admin/traffic-stats/subscriptions
 * @requires Authentication, Admin
 *
 * Returns paginated traffic statistics grouped by subscription.
 *
 * @param params - Query parameters with time range and pagination
 * @returns Paginated list of subscription traffic statistics
 *
 * @example
 * ```typescript
 * const subStats = await getSubscriptionTrafficStats({
 *   from: '2025-01-01T00:00:00Z',
 *   to: '2025-01-31T23:59:59Z',
 *   page: 1,
 *   pageSize: 20,
 * });
 * ```
 */
export const getSubscriptionTrafficStats = async (
  params: TrafficStatsQueryParams
): Promise<ListResponse<SubscriptionTrafficStatsItem>> => {
  const response = await apiClient.get<APIResponse<ListResponse<SubscriptionTrafficStatsItem>>>(
    '/admin/traffic-stats/subscriptions',
    { params }
  );
  return response.data.data;
};

/**
 * Get traffic statistics by node
 * GET /admin/traffic-stats/nodes
 * @requires Authentication, Admin
 *
 * Returns paginated traffic statistics grouped by node.
 *
 * @param params - Query parameters with time range and pagination
 * @returns Paginated list of node traffic statistics
 *
 * @example
 * ```typescript
 * const nodeStats = await getNodeTrafficStats({
 *   from: '2025-01-01T00:00:00Z',
 *   to: '2025-01-31T23:59:59Z',
 *   page: 1,
 *   pageSize: 20,
 * });
 * ```
 */
export const getNodeTrafficStats = async (
  params: TrafficStatsQueryParams
): Promise<ListResponse<NodeTrafficStatsItem>> => {
  const response = await apiClient.get<APIResponse<ListResponse<NodeTrafficStatsItem>>>(
    '/admin/traffic-stats/nodes',
    { params }
  );
  return response.data.data;
};

/**
 * Get user traffic ranking
 * GET /admin/traffic-stats/ranking/users
 * @requires Authentication, Admin
 *
 * Returns top N users by total traffic usage.
 *
 * @param params - Query parameters with time range and limit
 * @returns Traffic ranking response
 *
 * @example
 * ```typescript
 * const ranking = await getUserTrafficRanking({
 *   from: '2025-01-01T00:00:00Z',
 *   to: '2025-01-31T23:59:59Z',
 *   limit: 10,
 * });
 * ranking.items.forEach(item => {
 *   console.log(`#${item.rank} ${item.name}: ${formatTrafficBytes(item.total)}`);
 * });
 * ```
 */
export const getUserTrafficRanking = async (
  params: TrafficRankingParams
): Promise<TrafficRanking> => {
  const response = await apiClient.get<APIResponse<TrafficRanking>>(
    '/admin/traffic-stats/ranking/users',
    { params }
  );
  return response.data.data;
};

/**
 * Get subscription traffic ranking
 * GET /admin/traffic-stats/ranking/subscriptions
 * @requires Authentication, Admin
 *
 * Returns top N subscriptions by total traffic usage.
 *
 * @param params - Query parameters with time range and limit
 * @returns Traffic ranking response
 *
 * @example
 * ```typescript
 * const ranking = await getSubscriptionTrafficRanking({
 *   from: '2025-01-01T00:00:00Z',
 *   to: '2025-01-31T23:59:59Z',
 *   limit: 10,
 * });
 * ```
 */
export const getSubscriptionTrafficRanking = async (
  params: TrafficRankingParams
): Promise<TrafficRanking> => {
  const response = await apiClient.get<APIResponse<TrafficRanking>>(
    '/admin/traffic-stats/ranking/subscriptions',
    { params }
  );
  return response.data.data;
};

/**
 * Get traffic trend over time
 * GET /admin/traffic-stats/trend
 * @requires Authentication, Admin
 *
 * Returns traffic data grouped by time period (hour, day, or month).
 *
 * @param params - Query parameters with time range and granularity
 * @returns Traffic trend with time series data
 *
 * @example
 * ```typescript
 * const trend = await getTrafficTrend({
 *   from: '2025-01-01T00:00:00Z',
 *   to: '2025-01-31T23:59:59Z',
 *   granularity: 'day',
 * });
 * // Use with charting library
 * const chartData = trend.points.map(p => ({
 *   date: p.period,
 *   upload: p.upload,
 *   download: p.download,
 * }));
 * ```
 */
export const getTrafficTrend = async (
  params: TrafficTrendParams
): Promise<TrafficTrend> => {
  const response = await apiClient.get<APIResponse<TrafficTrend>>(
    '/admin/traffic-stats/trend',
    { params }
  );
  return response.data.data;
};

// ========== Admin Telegram Binding APIs (Added 2025-01-06) ==========

/**
 * Get admin Telegram binding status
 * GET /admin/telegram/binding
 * @requires Authentication, Admin
 *
 * Returns the current Telegram binding status for the authenticated admin.
 * If not bound, returns a verification code to use with the Telegram bot.
 *
 * @returns Binding status with optional verification code
 *
 * @example
 * ```typescript
 * const status = await getAdminTelegramBindingStatus();
 * if (status.isBound) {
 *   console.log('Bound to:', status.binding?.telegramUsername);
 * } else {
 *   console.log('Use code:', status.verifyCode);
 *   console.log('Bot link:', status.botLink);
 * }
 * ```
 */
export const getAdminTelegramBindingStatus = async (): Promise<AdminBindingStatusResponse> => {
  const response = await apiClient.get<APIResponse<AdminBindingStatusResponse>>(
    '/admin/telegram/binding'
  );
  return response.data.data;
};

/**
 * Unbind admin Telegram account
 * DELETE /admin/telegram/binding
 * @requires Authentication, Admin
 *
 * Removes the Telegram binding for the authenticated admin.
 * After unbinding, the admin will no longer receive Telegram notifications.
 *
 * @example
 * ```typescript
 * await unbindAdminTelegram();
 * console.log('Telegram unbound successfully');
 * ```
 */
export const unbindAdminTelegram = async (): Promise<void> => {
  await apiClient.delete('/admin/telegram/binding');
};

/**
 * Update admin notification preferences
 * PATCH /admin/telegram/preferences
 * @requires Authentication, Admin
 *
 * Updates the notification preferences for the authenticated admin's Telegram binding.
 * Only specified fields will be updated.
 *
 * @param data - Preferences to update
 * @returns Updated binding details
 *
 * @example
 * ```typescript
 * const updated = await updateAdminTelegramPreferences({
 *   notifyNodeOffline: true,
 *   notifyNewUser: false,
 *   offlineThresholdMinutes: 10,
 * });
 * console.log('Updated threshold:', updated.offlineThresholdMinutes);
 * ```
 */
export const updateAdminTelegramPreferences = async (
  data: UpdateAdminPreferencesRequest
): Promise<AdminTelegramBindingResponse> => {
  const response = await apiClient.patch<APIResponse<AdminTelegramBindingResponse>>(
    '/admin/telegram/preferences',
    data
  );
  return response.data.data;
};

// ========== System Settings APIs (Added 2025-01-06) ==========

/**
 * Get settings by category
 * GET /admin/settings/:category
 * @requires Authentication, Admin
 *
 * Returns all settings within the specified category.
 *
 * @param category - Setting category (e.g., "telegram", "system")
 * @returns Category settings response
 *
 * @example
 * ```typescript
 * const settings = await getCategorySettings('telegram');
 * settings.settings.forEach(s => {
 *   console.log(`${s.key}: ${s.value}`);
 * });
 * ```
 */
export const getCategorySettings = async (
  category: string
): Promise<CategorySettingsResponse> => {
  const response = await apiClient.get<APIResponse<CategorySettingsResponse>>(
    `/admin/settings/${category}`
  );
  return response.data.data;
};

/**
 * Update settings in a category
 * PUT /admin/settings/:category
 * @requires Authentication, Admin
 *
 * Batch updates settings within the specified category.
 *
 * @param category - Setting category
 * @param data - Settings to update (key-value pairs)
 *
 * @example
 * ```typescript
 * await updateCategorySettings('telegram', {
 *   settings: {
 *     enabled: true,
 *     bot_token: 'new_token',
 *   }
 * });
 * ```
 */
export const updateCategorySettings = async (
  category: string,
  data: UpdateCategorySettingsRequest
): Promise<void> => {
  await apiClient.put(`/admin/settings/${category}`, data);
};

/**
 * Get Telegram configuration
 * GET /admin/settings/telegram/config
 * @requires Authentication, Admin
 *
 * Returns the current Telegram bot configuration.
 * Sensitive values (bot_token, webhook_secret) are masked.
 *
 * @returns Telegram configuration
 *
 * @example
 * ```typescript
 * const config = await getTelegramConfig();
 * console.log('Mode:', config.mode);
 * console.log('Enabled:', config.enabled);
 * ```
 */
export const getTelegramConfig = async (): Promise<TelegramConfigResponse> => {
  const response = await apiClient.get<APIResponse<TelegramConfigResponse>>(
    '/admin/settings/telegram/config'
  );
  return response.data.data;
};

/**
 * Update Telegram configuration
 * PUT /admin/settings/telegram/config
 * @requires Authentication, Admin
 *
 * Updates the Telegram bot configuration.
 * Only specified fields will be updated.
 *
 * @param data - Configuration to update
 *
 * @example
 * ```typescript
 * await updateTelegramConfig({
 *   enabled: true,
 *   botToken: 'new_bot_token',
 *   webhookUrl: 'https://example.com/webhook',
 * });
 * ```
 */
export const updateTelegramConfig = async (
  data: UpdateTelegramConfigRequest
): Promise<void> => {
  await apiClient.put('/admin/settings/telegram/config', data);
};

/**
 * Test Telegram connection
 * POST /admin/settings/telegram/test
 * @requires Authentication, Admin
 *
 * Tests the Telegram bot connection using the current configuration.
 *
 * @returns Test result with success status and bot username
 *
 * @example
 * ```typescript
 * const result = await testTelegramConnection();
 * if (result.success) {
 *   console.log('Connected to bot:', result.botUsername);
 * } else {
 *   console.error('Connection failed:', result.error);
 * }
 * ```
 */
export const testTelegramConnection = async (): Promise<TelegramTestResult> => {
  const response = await apiClient.post<APIResponse<TelegramTestResult>>(
    '/admin/settings/telegram/test'
  );
  return response.data.data;
};

// ========== System Settings APIs (Added 2026-01-23) ==========

/**
 * Get system settings
 * GET /admin/settings/system
 * @requires Authentication, Admin
 *
 * Returns core system configuration values including API base URL,
 * subscription base URL, frontend URL, and timezone settings.
 *
 * @returns System settings with source information
 *
 * @example
 * ```typescript
 * const settings = await getSystemSettings();
 * console.log('API URL:', settings.apiBaseUrl.value);
 * console.log('Source:', settings.apiBaseUrl.source);
 * ```
 */
export const getSystemSettings = async (): Promise<SystemSettingsResponse> => {
  const response = await apiClient.get<APIResponse<SystemSettingsResponse>>(
    '/admin/settings/system'
  );
  return response.data.data;
};

/**
 * Update system settings
 * PUT /admin/settings/system
 * @requires Authentication, Admin
 *
 * Updates modifiable system settings.
 * Note: apiBaseUrl is read-only.
 *
 * @param data - Settings to update
 *
 * @example
 * ```typescript
 * await updateSystemSettings({
 *   subscriptionBaseUrl: 'https://subscribe.example.com',
 *   frontendUrl: 'https://app.example.com',
 * });
 * ```
 */
export const updateSystemSettings = async (
  data: UpdateSystemSettingsRequest
): Promise<void> => {
  await apiClient.put('/admin/settings/system', data);
};

// ========== OAuth Settings APIs (Added 2026-01-23) ==========

/**
 * Get OAuth settings
 * GET /admin/settings/oauth
 * @requires Authentication, Admin
 *
 * Returns OAuth configuration for all supported providers.
 * Sensitive values are masked.
 *
 * @returns OAuth settings for all providers
 *
 * @example
 * ```typescript
 * const oauth = await getOAuthSettings();
 * if (oauth.google.enabled) {
 *   console.log('Google Client ID:', oauth.google.clientId.value);
 * }
 * ```
 */
export const getOAuthSettings = async (): Promise<OAuthSettingsResponse> => {
  const response = await apiClient.get<APIResponse<OAuthSettingsResponse>>(
    '/admin/settings/oauth'
  );
  return response.data.data;
};

/**
 * Update OAuth settings
 * PUT /admin/settings/oauth
 * @requires Authentication, Admin
 *
 * Updates OAuth configuration for one or more providers.
 *
 * @param data - OAuth settings to update
 *
 * @example
 * ```typescript
 * await updateOAuthSettings({
 *   google: {
 *     clientId: 'new-client-id',
 *     clientSecret: 'new-client-secret',
 *   },
 * });
 * ```
 */
export const updateOAuthSettings = async (
  data: UpdateOAuthSettingsRequest
): Promise<void> => {
  await apiClient.put('/admin/settings/oauth', data);
};

// ========== Email Settings APIs (Added 2026-01-23) ==========

/**
 * Get email settings
 * GET /admin/settings/email
 * @requires Authentication, Admin
 *
 * Returns SMTP email configuration with source information.
 * Sensitive values are masked.
 *
 * @returns Email settings with source info
 *
 * @example
 * ```typescript
 * const email = await getEmailSettings();
 * console.log('SMTP Host:', email.smtpHost.value);
 * ```
 */
export const getEmailSettings = async (): Promise<EmailSettingsResponse> => {
  const response = await apiClient.get<APIResponse<EmailSettingsResponse>>(
    '/admin/settings/email'
  );
  return response.data.data;
};

/**
 * Update email settings
 * PUT /admin/settings/email
 * @requires Authentication, Admin
 *
 * Updates SMTP email configuration.
 *
 * @param data - Email settings to update
 *
 * @example
 * ```typescript
 * await updateEmailSettings({
 *   smtpHost: 'smtp.example.com',
 *   smtpPort: 587,
 * });
 * ```
 */
export const updateEmailSettings = async (
  data: UpdateEmailSettingsRequest
): Promise<void> => {
  await apiClient.put('/admin/settings/email', data);
};

/**
 * Test email connection
 * POST /admin/settings/email/test
 * @requires Authentication, Admin
 *
 * Sends a test email to verify SMTP configuration.
 *
 * @param data - Test request with recipient email
 * @returns Test result with success status
 *
 * @example
 * ```typescript
 * const result = await testEmailConnection({
 *   recipientEmail: 'admin@example.com',
 * });
 * if (result.success) {
 *   console.log('Test email sent successfully');
 * }
 * ```
 */
export const testEmailConnection = async (
  data: EmailTestRequest
): Promise<EmailTestResponse> => {
  const response = await apiClient.post<APIResponse<EmailTestResponse>>(
    '/admin/settings/email/test',
    data
  );
  return response.data.data;
};

// ========== Setup Status API (Added 2026-01-23) ==========

/**
 * Get setup status
 * GET /admin/settings/setup-status
 * @requires Authentication, Admin
 *
 * Checks if the system is properly configured for first-time setup.
 *
 * @returns Setup status with missing settings
 *
 * @example
 * ```typescript
 * const status = await getSetupStatus();
 * if (status.requiresSetup) {
 *   console.log('Missing settings:', status.missingSettings);
 * }
 * ```
 */
export const getSetupStatus = async (): Promise<SetupStatusResponse> => {
  const response = await apiClient.get<APIResponse<SetupStatusResponse>>(
    '/admin/settings/setup-status'
  );
  return response.data.data;
};

// ========== Admin Subscription Management APIs (Added 2025-01-14, Updated 2026-02-28) ==========

/**
 * List all subscriptions with filtering and pagination
 * GET /admin/subscriptions
 * @requires Authentication, Admin
 * Added: 2026-01-29
 *
 * Returns a paginated list of subscriptions with optional filtering.
 *
 * @param params - Query parameters for filtering and pagination
 * @returns Paginated list of subscriptions
 *
 * @example
 * ```typescript
 * // List all active subscriptions
 * const result = await listAdminSubscriptions({
 *   status: 'active',
 *   page: 1,
 *   pageSize: 20,
 * });
 *
 * // List subscriptions expiring soon
 * const expiringSoon = await listAdminSubscriptions({
 *   expiresBefore: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000).toISOString(),
 *   sortBy: 'end_date',
 *   sortOrder: 'asc',
 * });
 *
 * // List subscriptions by user
 * const userSubs = await listAdminSubscriptions({
 *   userId: 123,
 * });
 * ```
 */
export const listAdminSubscriptions = async (
  params?: AdminSubscriptionListParams
): Promise<ListResponse<AdminSubscriptionDTO>> => {
  const response = await apiClient.get<APIResponse<ListResponse<AdminSubscriptionDTO>>>(
    '/admin/subscriptions',
    { params }
  );
  return response.data.data;
};

/**
 * Get a single subscription by ID
 * GET /admin/subscriptions/:id
 * @requires Authentication, Admin
 * Added: 2026-01-29
 *
 * Returns detailed subscription information including plan and user.
 *
 * @param id - Subscription ID (Stripe-style SID like "sub_xxx" or numeric ID)
 * @returns Subscription details
 *
 * @example
 * ```typescript
 * const subscription = await getAdminSubscription('sub_xK9mP2vL3nQ');
 * console.log('Status:', subscription.status);
 * console.log('Plan:', subscription.plan?.name);
 * console.log('User:', subscription.user?.email);
 * ```
 */
export const getAdminSubscription = async (
  id: string
): Promise<AdminSubscriptionDTO> => {
  const response = await apiClient.get<APIResponse<AdminSubscriptionDTO>>(
    `/admin/subscriptions/${id}`
  );
  return response.data.data;
};

/**
 * Create a new subscription (Admin)
 * POST /admin/subscriptions
 * @requires Authentication, Admin
 * Added: 2026-01-29
 *
 * Creates a subscription for a user. Admin-created subscriptions
 * are activated by default (can be overridden with activate: false).
 *
 * @param data - Subscription creation request
 * @returns Created subscription with token
 *
 * @example
 * ```typescript
 * const result = await createAdminSubscription({
 *   userId: 'usr_abc123',
 *   planId: 'plan_premium456',
 *   billingCycle: 'monthly',
 *   autoRenew: true,
 * });
 * console.log('Subscription:', result.subscription.id);
 * console.log('Token:', result.token.token); // Save this - only shown once
 * ```
 */
export const createAdminSubscription = async (
  data: AdminCreateSubscriptionRequest
): Promise<AdminCreateSubscriptionResponse> => {
  const response = await apiClient.post<APIResponse<AdminCreateSubscriptionResponse>>(
    '/admin/subscriptions',
    data
  );
  return response.data.data;
};

/**
 * Delete a subscription
 * DELETE /admin/subscriptions/:id
 * @requires Authentication, Admin
 * Added: 2026-01-29
 *
 * Permanently deletes a subscription. This action cannot be undone.
 *
 * @param id - Subscription ID (Stripe-style SID like "sub_xxx" or numeric ID)
 *
 * @example
 * ```typescript
 * await deleteAdminSubscription('sub_xK9mP2vL3nQ');
 * console.log('Subscription deleted');
 * ```
 */
export const deleteAdminSubscription = async (id: string): Promise<void> => {
  await apiClient.delete(`/admin/subscriptions/${id}`);
};

/**
 * Update a subscription
 * PATCH /admin/subscriptions/:id
 * @requires Authentication, Admin
 * Added: 2026-02-28
 *
 * Updates subscription fields such as dates, traffic limit, and traffic usage.
 * All fields are optional. Use `null` for `dataLimitBytes` to clear the override.
 *
 * @param id - Subscription ID (Stripe-style SID like "sub_xxx" or numeric ID)
 * @param data - Fields to update
 * @returns Updated subscription details
 *
 * @example
 * ```typescript
 * // Set a custom traffic limit
 * const updated = await updateAdminSubscription('sub_xK9mP2vL3nQ', {
 *   dataLimitBytes: 107374182400, // 100 GB
 * });
 *
 * // Clear traffic limit override (use plan default)
 * const cleared = await updateAdminSubscription('sub_xK9mP2vL3nQ', {
 *   dataLimitBytes: null,
 * });
 * ```
 */
export const updateAdminSubscription = async (
  id: string,
  data: AdminUpdateSubscriptionRequest
): Promise<AdminSubscriptionDTO> => {
  const response = await apiClient.patch<APIResponse<AdminSubscriptionDTO>>(
    `/admin/subscriptions/${id}`,
    data
  );
  return response.data.data;
};

/**
 * Suspend a subscription
 * POST /admin/subscriptions/:id/suspend
 * @requires Authentication, Admin
 *
 * Suspends a subscription, preventing it from being used.
 * The subscription can be unsuspended later to restore access.
 *
 * @param id - Subscription ID (Stripe-style SID like "sub_xxx" or numeric ID)
 * @param data - Suspension details including reason
 *
 * @example
 * ```typescript
 * await suspendSubscription('sub_xK9mP2vL3nQ', {
 *   reason: 'Traffic limit exceeded',
 * });
 * ```
 */
export const suspendSubscription = async (
  id: string,
  data: SuspendSubscriptionRequest
): Promise<void> => {
  await apiClient.post(`/admin/subscriptions/${id}/suspend`, data);
};

/**
 * Unsuspend a subscription
 * POST /admin/subscriptions/:id/unsuspend
 * @requires Authentication, Admin
 *
 * Reactivates a suspended subscription, restoring access.
 *
 * @param id - Subscription ID (Stripe-style SID like "sub_xxx" or numeric ID)
 *
 * @example
 * ```typescript
 * await unsuspendSubscription('sub_xK9mP2vL3nQ');
 * console.log('Subscription reactivated');
 * ```
 */
export const unsuspendSubscription = async (id: string): Promise<void> => {
  await apiClient.post(`/admin/subscriptions/${id}/unsuspend`);
};

/**
 * Reset subscription usage
 * POST /admin/subscriptions/:id/reset-usage
 * @requires Authentication, Admin
 * Added: 2025-01-15
 *
 * Resets the subscription's traffic usage by updating the billing period start time
 * to now. This effectively clears all usage for the current period since usage is
 * calculated from period_start to period_end. The period_end remains unchanged to
 * maintain billing alignment.
 *
 * If the subscription is suspended (e.g., due to traffic limit exceeded), it will
 * be automatically unsuspended.
 *
 * @param id - Subscription ID (Stripe-style SID like "sub_xxx" or numeric ID)
 *
 * @example
 * ```typescript
 * await resetSubscriptionUsage('sub_xK9mP2vL3nQ');
 * console.log('Subscription usage reset successfully');
 * ```
 */
export const resetSubscriptionUsage = async (id: string): Promise<void> => {
  await apiClient.post(`/admin/subscriptions/${id}/reset-usage`);
};

/**
 * Renew a subscription
 * POST /admin/subscriptions/:id/renew
 * @requires Authentication, Admin
 * Added: 2026-01-29
 *
 * Manually renews a subscription for another billing period.
 * If billingCycle is not provided, uses the subscription's current billing cycle.
 *
 * **Allowed statuses:** `active`, `past_due`, `expired`
 * **Not allowed:** `suspended`, `cancelled`, `inactive`, `pending_payment`, `trialing`
 * **Note:** Lifetime subscriptions cannot be renewed.
 *
 * @param id - Subscription ID (Stripe-style SID like "sub_xxx" or numeric ID)
 * @param data - Optional renewal details including billing cycle
 * @throws Error if subscription status doesn't allow renewal
 *
 * @example
 * ```typescript
 * // Renew using current billing cycle
 * await renewSubscription('sub_xK9mP2vL3nQ');
 *
 * // Renew with a different billing cycle
 * await renewSubscription('sub_xK9mP2vL3nQ', {
 *   billingCycle: 'yearly',
 * });
 * ```
 */
export const renewSubscription = async (
  id: string,
  data?: RenewSubscriptionRequest
): Promise<void> => {
  await apiClient.post(`/admin/subscriptions/${id}/renew`, data ?? {});
};

/**
 * Change subscription plan
 * PATCH /admin/subscriptions/:id/plan
 * @requires Authentication, Admin
 * Added: 2026-01-29
 *
 * Changes the subscription to a different plan. Can be applied immediately
 * or scheduled to take effect at the end of the current billing period.
 *
 * **Allowed status:** `active` only
 * **Not allowed:** All other statuses (expired, cancelled, suspended, etc.)
 *
 * @param id - Subscription ID (Stripe-style SID like "sub_xxx" or numeric ID)
 * @param data - Plan change details
 * @throws Error if subscription is not active
 *
 * @example
 * ```typescript
 * // Upgrade immediately
 * await changeSubscriptionPlan('sub_xK9mP2vL3nQ', {
 *   newPlanId: 'plan_premium123',
 *   changeType: 'upgrade',
 *   effectiveDate: 'immediate',
 * });
 *
 * // Downgrade at period end
 * await changeSubscriptionPlan('sub_xK9mP2vL3nQ', {
 *   newPlanId: 'plan_basic456',
 *   changeType: 'downgrade',
 *   effectiveDate: 'period_end',
 * });
 * ```
 */
export const changeSubscriptionPlan = async (
  id: string,
  data: ChangePlanRequest
): Promise<void> => {
  await apiClient.patch(`/admin/subscriptions/${id}/plan`, data);
};

// Note: Admin External Forward Rules APIs have been removed (2026-01-20)
// External forward rules are now part of the unified ForwardRule model with rule_type='external'
// Use the forward module's Admin APIs with ruleType='external' filter instead:
// - listForwardRules({ ruleType: 'external' })
// - createForwardRule({ ruleType: 'external', ... })
// etc.
