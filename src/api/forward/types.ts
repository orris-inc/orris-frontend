/**
 * AUTO-GENERATED - DO NOT EDIT
 * Generated by Claude from backend Go code
 * Source: orris backend
 *
 * Note: All properties use camelCase. The axios-case-converter middleware
 * automatically converts between snake_case (API) and camelCase (frontend).
 */

// ========== Forward Rule Types ==========

/**
 * Forward rule entity
 */
export interface ForwardRule {
  id: number;
  agentId: number;
  nextAgentId: number; // 0=direct forward, >0=chain forward to next agent
  isChain: boolean; // convenience field
  name: string;
  listenPort: number;
  targetAddress?: string; // used when nextAgentId=0
  targetPort?: number; // used when nextAgentId=0
  protocol: ForwardProtocol;
  status: ForwardStatus;
  remark: string;
  uploadBytes: number;
  downloadBytes: number;
  totalBytes: number;
  createdAt: string;
  updatedAt: string;
}

/**
 * Forward protocol type
 */
export type ForwardProtocol = 'tcp' | 'udp' | 'both';

/**
 * Forward status type
 */
export type ForwardStatus = 'enabled' | 'disabled';

/**
 * Create forward rule request
 */
export interface CreateForwardRuleRequest {
  name: string;
  listenPort: number;
  targetAddress: string;
  targetPort: number;
  protocol: ForwardProtocol;
  remark?: string;
}

/**
 * Update forward rule request
 */
export interface UpdateForwardRuleRequest {
  name?: string;
  listenPort?: number;
  targetAddress?: string;
  targetPort?: number;
  protocol?: ForwardProtocol;
  remark?: string;
}

/**
 * Update forward rule status request
 */
export interface UpdateForwardRuleStatusRequest {
  status: ForwardStatus;
}

/**
 * List forward rules params
 */
export interface ListForwardRulesParams {
  page?: number;
  pageSize?: number;
  name?: string;
  protocol?: ForwardProtocol;
  status?: ForwardStatus;
  orderBy?: string;
  order?: 'asc' | 'desc';
}

// ========== Forward Agent Types ==========

/**
 * Forward agent entity
 */
export interface ForwardAgent {
  id: number;
  name: string;
  description?: string;
  status: ForwardStatus;
  remark: string;
  token?: string;
  lastSeenAt?: string;
  createdAt: string;
  updatedAt: string;
}

/**
 * Forward agent with token (returned on creation)
 */
export interface ForwardAgentWithToken extends ForwardAgent {
  token: string;
}

/**
 * Create forward agent request
 */
export interface CreateForwardAgentRequest {
  name: string;
  description?: string;
  remark?: string;
}

/**
 * Update forward agent request
 */
export interface UpdateForwardAgentRequest {
  name?: string;
  description?: string;
  remark?: string;
}

/**
 * Update forward agent status request
 */
export interface UpdateForwardAgentStatusRequest {
  status: ForwardStatus;
}

/**
 * List forward agents params
 */
export interface ListForwardAgentsParams {
  page?: number;
  pageSize?: number;
  name?: string;
  status?: ForwardStatus;
  orderBy?: string;
  order?: 'asc' | 'desc';
}

/**
 * Regenerate token response
 */
export interface RegenerateTokenResponse {
  id: number;
  token: string;
}

// ========== Forward Chain Types ==========

/**
 * Chain node entity
 */
export interface ChainNode {
  agentId: number;
  listenPort: number;
  sequence: number;
}

/**
 * Forward chain entity
 */
export interface ForwardChain {
  id: number;
  name: string;
  protocol: ForwardProtocol;
  status: ForwardStatus;
  nodes: ChainNode[];
  nodeCount: number;
  targetAddress: string;
  targetPort: number;
  remark: string;
  createdAt: string;
  updatedAt: string;
}

/**
 * Chain node request
 */
export interface ChainNodeRequest {
  agentId: number;
  listenPort: number;
}

/**
 * Create forward chain request
 */
export interface CreateForwardChainRequest {
  name: string;
  protocol: ForwardProtocol;
  nodes: ChainNodeRequest[];
  targetAddress: string;
  targetPort: number;
  remark?: string;
}

/**
 * Update forward chain status request
 */
export interface UpdateForwardChainStatusRequest {
  status: ForwardStatus;
}

/**
 * List forward chains params
 */
export interface ListForwardChainsParams {
  page?: number;
  pageSize?: number;
  name?: string;
  status?: ForwardStatus;
  orderBy?: string;
  order?: 'asc' | 'desc';
}
