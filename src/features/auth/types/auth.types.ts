/**
 * 认证相关类型定义
 * 基于后端 Swagger 文档: backend/swagger.yaml
 *
 * 注意：User、LoginRequest、RegisterRequest 等基础类型已迁移至 @/api/auth
 * 此文件仅保留本地特有的扩展类型
 */

/**
 * 本文件中的所有类型定义已迁移至 @/api/auth
 * 如需使用认证相关类型，请从 @/api/auth 导入
 *
 * 示例:
 * import type { User, LoginRequest, OAuthProvider } from '@/api/auth';
 */
